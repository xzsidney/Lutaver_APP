<nav class="player-top-navbar">
    <div class="navbar-container">
        <!-- Left: Logo/Title -->
        <div class="navbar-left">
            <span class="nav-logo">🎮</span>
            <a href="/player/dashboard" class="nav-title">Área do Jogador</a>
        </div>

        <!-- Center: Navigation Icons -->
        <div class="navbar-center">
            <a href="#videos" class="nav-icon-item">
                <div class="icon-circle">▶️</div>
                <span class="icon-label">Vídeos</span>
                <a href="#atividades" class="nav-icon-item">
                    <div class="icon-circle">📝</div>
                    <span class="icon-label">Atividades</span>
                </a>
        </div>

        <!-- Right: User Profile -->
        <div class="navbar-right">
            <div class="user-profile-badge">
                <div class="user-avatar-circle">
                    <%= user.name.charAt(0).toUpperCase() %>
                </div>
                <span class="user-name-text">
                    <%= user.name %>
                </span>
                <a href="/player/account/profile" class="settings-icon" title="Configurações">⚙️</a>
            </div>
        </div>
    </div>
</nav>

<style>
    /* Player Top Navbar - Based on Reference Image */
    .player-top-navbar {
        background: #FFFFFF;
        border-bottom: 3px solid #2196F3;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        position: sticky;
        top: 0;
        z-index: 1000;
        padding: 12px 0;
    }

    .navbar-container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 0 30px;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    /* Left Section */
    .navbar-left {
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .nav-logo {
        font-size: 2rem;
    }

    .nav-title {
        font-size: 1.3rem;
        font-weight: 700;
        color: #1976D2;
        text-decoration: none;
        transition: 0.2s;
    }

    .nav-title:hover {
        color: #1565C0;
    }

    /* Center Section - Icon Navigation */
    .navbar-center {
        display: flex;
        gap: 25px;
        align-items: center;
    }

    .nav-icon-item {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 6px;
        text-decoration: none;
        transition: 0.2s ease-in-out;
        cursor: pointer;
    }

    .nav-icon-item:hover {
        transform: translateY(-3px);
    }

    .icon-circle {
        width: 50px;
        height: 50px;
        background: #F5F5F5;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        transition: 0.2s ease-in-out;
    }

    .nav-icon-item:hover .icon-circle {
        background: #E3F2FD;
        box-shadow: 0 4px 10px rgba(33, 150, 243, 0.3);
    }

    .icon-label {
        font-size: 0.8rem;
        font-weight: 600;
        color: #424242;
    }

    /* Right Section - User Profile */
    .navbar-right {
        display: flex;
        align-items: center;
    }

    .user-profile-badge {
        display: flex;
        align-items: center;
        gap: 10px;
        background: #F5F5F5;
        padding: 8px 16px;
        border-radius: 50px;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.08);
    }

    .user-avatar-circle {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: #FFFFFF;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.2rem;
        font-weight: 700;
        box-shadow: 0 2px 6px rgba(102, 126, 234, 0.4);
    }

    .user-name-text {
        font-size: 0.95rem;
        font-weight: 600;
        color: #424242;
    }

    .settings-icon {
        font-size: 1.3rem;
        text-decoration: none;
        transition: 0.2s;
        cursor: pointer;
    }

    .settings-icon:hover {
        transform: rotate(45deg) scale(1.1);
    }

    /* Responsive Design */
    @media (max-width: 1024px) {
        .navbar-center {
            gap: 15px;
        }

        .icon-circle {
            width: 45px;
            height: 45px;
            font-size: 1.3rem;
        }

        .icon-label {
            font-size: 0.75rem;
        }
    }

    @media (max-width: 768px) {
        .navbar-container {
            flex-direction: column;
            gap: 15px;
            padding: 0 15px;
        }

        .navbar-center {
            order: 2;
            flex-wrap: wrap;
            justify-content: center;
            gap: 12px;
        }

        .navbar-right {
            order: 1;
        }

        .nav-title {
            font-size: 1.1rem;
        }
    }

    @media (max-width: 480px) {
        .icon-circle {
            width: 40px;
            height: 40px;
            font-size: 1.2rem;
        }

        .icon-label {
            display: none;
        }

        .user-name-text {
            display: none;
        }
    }
</style>